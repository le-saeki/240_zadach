#include <iostream>
using namespace std;

int maxmin1(int*arr, int first, int last)
{
	int min = arr[0];
	
	for(; first<last;++first)
	{
		if(arr[first]<min)
	{	 
		min=arr[first];
	}
	}
 	return min ;

}

int main()
{
	int array[5]{2*4,3*3,5*6,4*4,3*5};
	cout<<maxmin1(array,0,5);
	return 0;
}
